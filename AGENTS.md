# Agent skills

### Issue tracker

Issues live in leafac/kill-the-newsletter's GitHub Issues (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context repo — `CONTEXT.md` + `docs/adr/` at the root (created lazily). See `docs/agents/domain.md`.
